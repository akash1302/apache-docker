version: 2
jobs:
 apache_deploy:
   machine: true
   steps:
 #  - run:
  #  command: |
   #    ssh-keyscan 13.59.53.222 >> ~/.ssh/known_hosts

     - run: |
             sudo apt update && sudo apt install sshpass -y
             sshpass -p $USER_PASS1 ssh spineor@192.168.43.6 'ls' 'ltr'
             
           
             
workflows:
 version: 2
 rolling_update:
   jobs:
     - apache_deploy:
         filters:
           branches:
             only:
               - master
